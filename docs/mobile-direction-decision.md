# Mobile Direction Decision

## Decision
Yes - Math3D mobile should be a **native React app** using **React Native**.

## Implementation Base
1. Use the existing `apps/mobile` Expo + React Native workspace.
2. Continue as a migration from the current shell app to a production companion app.

## Clarification
1. This is **not** a new stack decision (already chosen).
2. This is a **feature and architecture implementation phase** on top of the existing React Native base.
