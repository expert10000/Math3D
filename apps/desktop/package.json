{
  "name": "@math3d/app-desktop",
  "private": true,
  "version": "1.4.6-beta.7",
  "scripts": {
    "dev": "npm run dev --prefix ../..",
    "build": "npm run build:core --prefix ../..",
    "dist": "npm run dist --prefix ../.."
  }
}
