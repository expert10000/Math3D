# GitDiagram First Pass

GitDiagram quick link for this repository:

- https://gitdiagram.com/expert10000/Math3D

How it is derived:

- Replace `github.com` with `gitdiagram.com` in the public repo URL.
- Example: `https://github.com/expert10000/Math3D` -> `https://gitdiagram.com/expert10000/Math3D`

When to use:

- Fast interactive first-pass overview of modules and structure.
- Less suitable for precise architecture boundaries (use dependency-cruiser and Arkit artifacts in `analysis/dependency` for rigor).
